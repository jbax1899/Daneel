{
    "name": "frontend",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "build": "mkdir -p build && cp -r src/pages/* build/ && cp -r src/styles build/",
      "start": "serve -s build -l 3000"
    },
    "dependencies": {
      "serve": "^14.2.0"
    }
  }